// This is used to initialize the screen elements
var DisplayModel = function (id, options) {
     this.options = {
            height: '100px',
            width : '100%',
            value:0
     };
     this.id=id;
}